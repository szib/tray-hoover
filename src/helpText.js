export default `
The current folder should have a file named 'input.txt' in the following format.

● the first line holds the room dimensions
● the second line holds the hoover position
● subsequent lines contain the zero or more positions of patches of dirt (one per line)
● the next line then always contains the driving instructions (at least one)

All coordinates should be presented as two numbers separate with a space.

Example:
5 5
1 2
1 0
2 2
2 3 
NNESEESWNWW
`;
